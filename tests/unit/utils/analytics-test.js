import analytics from 'steganodb-admin/utils/analytics';
import { module, test } from 'qunit';

module('Unit | Utility | analytics');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = analytics();
  assert.ok(result);
});
